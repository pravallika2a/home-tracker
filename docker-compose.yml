services:
  home-tracker:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - app-data:/data
    environment:
      - DATA_PATH=/data
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  app-data:
